import React from 'react';

const Lottie = ({ animationData, loop, autoplay, style }: any) => {
  return (
    <div 
      style={style}
      className="lottie-mock"
      data-testid="lottie-mock"
    >
      {/* Mock Lottie animation */}
    </div>
  );
};

export default Lottie;